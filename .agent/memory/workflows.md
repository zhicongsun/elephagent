# Workflows

Record repeatable project workflows here.

## Agent Memory Workflow

1. Edit `.agent/memory/*.md` or run `python3 agent_kit.py remember "..."`.
2. Run `python3 agent_kit.py build`.
3. Commit `.agent/` and the generated adapter files.
